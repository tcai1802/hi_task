enum ImageTypeEnum {
  netWork,
  cachedNetWork,
  file,
  assetFile,
  svgPicture,
}

enum NavbarEnum {
  home,
  calendarPriority,
  profile,
}

enum StateEnum { init, loading, success, failed }

enum TaskTypeEnum { dailyTask, priorityTask }
