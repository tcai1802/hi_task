import 'package:flutter/material.dart';

class CalendarPriorityScreen extends StatelessWidget {
  const CalendarPriorityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("CalendarPriorityScreen"),
    );
  }
}
